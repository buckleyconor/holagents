---
module_n: 1
slug: fill-module-1
title: '<< FILL: Module title >>'
depends_on: []
est_minutes: 15
image_checklist:
  - '<< FILL: every screenshot the module needs — what the reader should see, e.g. RAG UI showing the New Collection button >>'
  - '<< FILL: screenshot 2 — what the reader should see >>'
success_criteria:
  - '<< FILL: verifiable end state — state the observable outcome, not the action >>'
  - '<< FILL: verifiable end state 2 >>'
---

## Step outline (numbered; each step: action, expected result, screenshot?)

1. << FILL: action — name the UI control in bold with quotes, e.g. Click the **"New Collection"** button. >> — expected: << FILL: observable result >> — screenshot: << FILL: yes/no; if yes, which image_checklist item >>
2. << FILL: step 2 >> — expected: << FILL: observable result >> — screenshot: << FILL: yes/no; if yes, which image_checklist item >>

## Environment delta

<< FILL: what this module assumes is already true (baseline plus earlier
modules) and what it leaves behind for the next module. The environment is
pre-provisioned — record no setup steps here. >>

## Commands used (full command text, in backtick form)

    `<< FILL: full command exactly as it will appear in the guide, e.g. curl -s http://localhost:8090/health >>`

## Expected outputs (verbatim sample output where known)

<< FILL: verbatim sample output captured during the dry run, or "not known yet — capture during the dry run". >>
