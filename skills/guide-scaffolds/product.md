# << FILL: product name, exact spelling >>

Company: << FILL: company slug >>

## Overview

<< FILL: 2–4 sentences — what it is and the problem it solves. Specifics over
marketing fluff; a lab guide must be writable from this section alone. >>

## Key features

- << FILL: feature — what it does concretely, and how a learner would touch it >>

## Use cases

- << FILL: realistic use case a lab guide could model >>

## Setup requirements

<< FILL: what must be true before a first session (versions, licenses,
accounts). Lab environments are pre-provisioned — list the end state, not
install steps. >>

## Core workflows

The 2–3 workflows a lab guide would teach, in learner order:

1. **<< FILL: workflow name >>** — << FILL: entry point → exit state >>
2. **<< FILL: workflow name >>** — << FILL: entry point → exit state >>

## Concepts to teach

- << FILL: term — one-line definition in the learner's context >>

## Console/CLI entry points

| Surface                             | Entry point                | Default host:port     |
| ----------------------------------- | -------------------------- | --------------------- |
| << FILL: web console / CLI / API >> | << FILL: URL or command >> | << FILL: host:port >> |

## Default hosts, ports & credential surfaces

- << FILL: e.g. web console at https://localhost:8090, user `demouser` — must match the guide's Lab Credentials block >>

## Documentation links

- << FILL: URL — why it matters for guide authoring >>

## Sources

Pages analyzed (under `website/` in this directory):

- << FILL: page path or URL >> — << FILL: what was taken from it >>
